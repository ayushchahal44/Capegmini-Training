package com.capg.ayush.notification;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * Placeholder test — actual tests are in service/ and messaging/ packages.
 * Full integration tests require RabbitMQ and are run separately.
 */
class NotificationServiceTest {

    @Test
    void contextPlaceholder() {
        // Real tests are in NotificationServiceUnitTest, NotificationControllerTest, and StatusEventListenerTest
        assertTrue(true);
    }
}
